Let’s analyze step by step:

int a = 0;
for (i = 0; i < N; i++) {          // Outer loop runs N times
    for (j = N; j > i; j--) {      // Inner loop runs (N - i) times
        a = a + i + j;             // O(1) work
    }
}

⏱ Time Complexity

Outer loop (i) → runs N times.

Inner loop (j) → for each i, runs from N down to i+1, i.e. (N - i) times.
	​
(N−i)=N+(N−1)+(N−2)+⋯+1

✅ Correct Answer:

O(N * N)
