# Define the main function
def main():
    # Print the string "InterviewBit" repeated 100 times
    # In Python, multiplying a string by an integer repeats it that many times
    print("InterviewBit" * 100)

    # Return 0 to indicate successful completion (optional in Python)
    return 0


# This checks if the script is being run directly (not imported as a module)
if __name__ == '__main__':
    main()
